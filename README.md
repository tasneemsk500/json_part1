# json_part1
Create a function named findMostFrequentItem that takes an object where keys are item names and values are quantities. The function should:  Find and return the name of the item with the highest quantity If there's a tie, return the first item that appears in the object
